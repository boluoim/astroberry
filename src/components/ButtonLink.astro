---
import { Icon } from "astro-icon/components";

interface Props {
    href: string;
    class?: string;
}

const props = Astro.props;
---

<div class:list={[props.class]}>
    <a class="inline-flex" href={props.href}>
        <span class="px-3 py-1 border-[1px] rounded-l text-sm">
            <slot />
        </span>
        <span class="p-2 border-[1px] border-l-0 rounded-r">
            <Icon name="mdi:arrow-right" class="w-3 h-3 cursor-pointer hover:text-brand" />
        </span>
    </a>
</div>